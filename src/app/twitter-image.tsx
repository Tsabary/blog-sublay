// Twitter/X share image. Reuses the same template + content as the OpenGraph
// card so both surfaces show an identical, always-current image.
export { default, alt, size, contentType } from "./opengraph-image";
